// Interface for the input the compiler expects
export interface IFilePinResponse {
    ipfsHash: string;
    ipfsUri: string;
}

export interface IJsonPinResponse {
    ipfsHash: string;
    ipfsUri: string;
}
